# AGENTS.md

本文件约束 MoeKoeMusic Mobile V2 中的人类贡献者和编码代理。开始修改前先阅读 `README.md` 与任务相关的 `docs/` 文档。

## 工作原则

- 优先小而可审查的改动，不做未经要求的大规模重构。
- 修改前明确目标文件、边界和验证方式。
- 不虚构酷狗接口、字段、签名常量或错误码；不确定时回到固定版本的 KuGouMusicApi 查证。
- 保持 Android 原生实现，不引入 Node、内嵌 HTTP 服务、WebView API 桥或 JavaScript Runtime。
- 新依赖必须说明用途、维护状态、许可证和无法使用现有依赖完成的原因。
- 不添加分析、遥测、广告或额外网络请求，除非需求明确要求。
- 每个已经完成并通过对应验证的可审查切片必须立即形成原子提交；提交只包含该切片，不把已完成工作长期留在工作区，也不夹带其他任务变更。

## 开发前技术审计门禁

- 重大功能、模块边界、权限、存储、后台任务、协议迁移和核心 UI 系统在编码前必须完成详细功能规格与技术参考审计。
- 审计先以 Android 官方文档确认平台约束，再检查至少两个成熟、仍有参考价值的开源项目；必须固定提交、列出具体文件，并记录采用点、不采用点和许可证结论。
- 审计结果写入 `docs/reference-audits/`，不得只保留在 Issue、聊天或临时目录中。
- 已提供本地固定版本参考源码时，必须先读取对应 Endpoint、调用层和消费层源码，以源码确定请求、响应兼容和业务分支；不得用真实服务探针代替已有源码阅读。真实服务只用于实现完成后的兼容性验收。仅当固定源码缺失、相互冲突或服务行为已漂移时才做最小探针，并记录原因与结论。
- 只有产品语义、技术栈、数据流、失败恢复和测试矩阵均无关键待定项时，阶段才能标记为“进行中”并开始业务代码。
- 小型缺陷、文案、纯机械修改可以豁免，但不得借豁免改变架构或引入新依赖。
- 第三方实现只能作为证据和设计参考；复制或迁移代码前必须确认与 GPL-2.0-only 兼容并保留归属。

## UI 设计门禁

- 任何新增或实质改变用户可见页面、组件、导航、覆盖层或交互状态的功能，在原型和 Compose 编码前必须具备对应设计图；只有纯后台能力和不改变布局的小型文案、机械修复可以豁免。
- 功能规格必须先列出正常、加载、空数据、错误、离线、权限、未登录、账号受限、取消和恢复等适用状态，再逐项链接覆盖它们的设计图；只有主状态、缺少异常或恢复状态不视为设计完整。
- 缺少设计图时，先使用 `frontend-design` 在现有 MoeKoe Air 视觉、`DESIGN_SYSTEM.md` 和已批准页面约束内补图，不得另起一套色板、字体、信息架构或组件语言。
- 补充同一页面的新状态时，已确认主状态不是抽象风格参考，而是必须直接复用的页面基座；状态图只能替换规格明确允许变化的内容区域。状态栏、Hero、页面容器、固定导航、通用控件及其尺寸和坐标不得由生图模型重新解释。确需切换到独立 Activity 或其他页面作为基座时，必须在状态映射中明确记录例外及其权威设计图。
- 固定顺序为“视觉约束审计 → 静态设计图生成 → 用户确认 → 必要时制作交互原型 → 文档与 Design System 同步 → Compose 实现”。设计图未生成或未确认时，不得提前制作原型；候选图、原型和自动生成图也不得自行标记为实现基线。
- 原型只验证已确认设计的交互、滚动、转场和响应式行为，不能替代静态设计图门禁。设计图中的自动生成文案和标注不作为协议、产品文案或 Token 真值。
- Dialog、Bottom Sheet 等覆盖层状态必须直接复用对应原页面画布，再依次叠加规范遮罩与独立前景层；不得让生图模型重绘遮罩后的原页面。除覆盖层明确造成的遮挡外，底层页面的文字、控件、输入草稿、尺寸和坐标必须保持不变。
- 已确认设计图是页面结构、层级、密度和视觉形态的权威依据；原型只作为交互参考。原型中的字符图标、近似图标和临时装饰不得复制到 Compose，正式图标必须按设计图语义使用固定版本的 Material Icons 或已批准品牌资源。
- 设计确认后必须更新 `docs/design/mockups/README.md`、相关阶段计划和唯一页面映射；实现中发现新状态时，先补设计并重新确认，再继续布局代码。

## 架构边界

- Compose UI 不直接访问网络、数据库、DataStore、ExoPlayer 或 Android Service。
- ViewModel 只暴露不可变 `StateFlow` UI 状态和明确的用户事件方法。
- `:playback` 是唯一能够创建和持有 ExoPlayer 的模块。
- `MediaLibraryService` 不承担搜索、登录、收藏、歌词解析等产品业务。
- `:kugou-api` 只负责酷狗协议、会话、签名、加密、传输和网络 DTO，不依赖 Compose 或 Media3。
- 网络 DTO、数据库 Entity、领域 Model 和 UI Model 不混用。
- UseCase 仅用于跨 Repository 或具有明确业务规则的操作，不为每个简单读取机械创建 UseCase。

## Compose 规范

- Route 负责获取 ViewModel 和收集状态，Screen 尽量保持无状态。
- 可复用组件不得自行导航或读取全局单例。
- Composable 参数优先传值和事件 lambda，不传整个 ViewModel。
- 列表项必须提供稳定 key；高频进度状态不得导致整个页面重组。
- 所有可交互图标提供无障碍语义或 `contentDescription`。
- 新页面同时考虑加载、空数据、错误、离线和正常状态。
- UI token 统一来自 `:core:designsystem`，禁止在业务页面散落品牌色和重复尺寸常量。

## Kotlin 规范

- 使用 Kotlin 官方代码风格和显式空值处理。
- 避免 `!!`、全局可变状态和不受控的 `GlobalScope`。
- 协程作用域必须有明确生命周期；IO 与计算调度器可注入以便测试。
- 对可恢复错误使用明确的领域错误类型，不使用字符串判断业务状态。
- 公共 API 优先返回稳定领域类型；动态 JSON 容错限制在网络层。
- 注释解释意图和协议怪异点，不复述代码表面行为。

## 安全与协议

- 不提交账号 token、Cookie、设备标识、私钥、`.env` 或真实抓包数据。
- 测试向量使用固定的虚构身份数据，并在文档中说明来源。
- 登录凭据和敏感会话使用 Android Keystore 支持的加密存储。
- 日志必须脱敏，不输出完整 token、手机号、Cookie、播放签名或服务端验证载荷。
- 保留 KuGouMusicApi MIT 许可证和迁移来源说明。

## 测试与验证

- 加密、签名和请求参数迁移必须有 Node 与 Kotlin 对照测试。
- Repository、队列状态机和播放模式变化必须有单元测试。
- UI 行为变化至少验证对应 Preview、截图测试或设备测试之一。
- 先运行最快的相关检查，再运行受影响模块的完整测试。
- 当前开发验收只使用用户指定且已连接的真机，不启动或创建 Android 模拟器；历史模拟器结果可以保留为既有记录，但不能冒充本轮验收。
- 提交结果时说明执行过的命令、通过情况和未执行原因。

## 文档同步

- 改变模块边界时更新 `docs/ARCHITECTURE.md`。
- 改变接口迁移策略时更新 `docs/API_MIGRATION.md` 和相关 ADR。
- 确定 UI 设计决策时更新 `docs/UI_DESIGN_BRIEF.md`。
- 增加重要外部参考时更新 `docs/REFERENCES.md`，记录借鉴点和不采用点。
