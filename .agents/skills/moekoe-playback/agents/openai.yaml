interface:
  display_name: "MoeKoe 播放内核"
  short_description: "维护 Media3 播放、队列、恢复与播放器状态边界"
  default_prompt: "Use $moekoe-playback to plan or review this MoeKoeMusic playback change."
policy:
  allow_implicit_invocation: true
