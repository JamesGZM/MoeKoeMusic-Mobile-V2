# “我的”未登录状态候选稿 v2

状态：候选，等待用户确认。

- [`04-my-anonymous-v2.png`](04-my-anonymous-v2.png)：以已确认的 [`04-my-v4.png`](../../04-my-v4.png) 为唯一页面视觉与几何基准，通过 `frontend-design` 以 HTML/CSS 确定性绘制。
- [`index.html`](index.html)：设计源稿，仅用于生成静态候选图，不是生产 Web 页面或 Android 实现。
- 顶部继续保留 `04-my-v4` 的“资料区 + 签到 + 领取 VIP”结构；匿名态将资料区替换为登录引导，并把两项权益明确标记为登录后可用。
- 本地音乐与设置保持可用；账号资产不展示虚构计数；MiniPlayer 与底部导航保持原页面信息层级。
- 本候选不参考现有 Compose 匿名态截图，也不代表现有实现已经符合设计。

本图在明确确认并同步唯一页面映射前，不是 Compose 实现基线。
