# 设置页设计符合度证据

- 确认稿：`docs/design/mockups/09-settings.png`
- 实现截图：`feature/settings/src/screenshotTestDebug/reference/cn/james/music/feature/settings/SettingsScreenshotTestKt/SettingsLightScreenshot_Light_442d9280_0.png`
- 比较范围：确认稿 `853 × 2172px` 按实现截图宽度归一化，再裁取同高首屏对照；设置稿本身不绘制系统状态栏，完整长内容由 `SettingsLongContentScreenshot` 和真机滚动测试验收。公共 Toolbar 的标题、图标和光学基线另以 `10-user-profile.png` 母版复核。

## 证据文件

- 当前机器证据：`build/reports/ui-evidence/settings.content.light/`，由 `verifyUiFidelity -Pmoekoe.uiContract=settings.content.light` 确定性生成。
- `settings-2026-08-08-side-by-side.png`、`settings-2026-08-08-overlay.png`、`settings-2026-08-08-diff.png` 仅保留为引入真实“播放失败时自动跳过”开关之前的历史人工证据，不代表当前渲染。
- 2026-08-10 默认音质接入后，`settings.content.light` 当轮机器符合度为 `mean=0.028692134630664817`、`changed=0.049485659561118174`，在既有阈值 `0.16 / 0.50` 内；契约未放宽阈值或扩大遮罩。人工并排/overlay 确认默认音质行仅将旧不可提交尾值替换为“标准”，分组、图标、Switch 与其余锚点未漂移。
- 2026-08-10 主题色 Settings 代码后，`settings.content.light` 使用最新 rendered 的当轮机器符合度为 `mean=0.02877464763021309`、`changed=0.04950407649250177`，在既有阈值 `0.16 / 0.50` 内；契约将主题色尾值登记为独立 `brandThemeColorTrailing` region，没有放宽阈值或扩大遮罩。人工查看确认默认浅色状态的新增差异集中于主题色行的“天空蓝”尾值，五种非蓝预设改变的仅是既有 primary 角色及主题色尾值；主题色 Dialog `2.0×` 的标题、六个选项和取消按钮完整可见。经父级逐项授权，仅更新 Light/Dark/AMOLED/两档大字体/LongContent/LayoutProbe/两个既有 Dialog 共 9 张受影响基线，并新增五种非蓝及主题色 Dialog `1.0×` / `2.0×` 共 7 张基线；未更新 `PlaybackQualityDialogLargeText20` 或其他 PNG。
- 2026-08-10 清理缓存进入编码前契约：既有 `dynamic.storageTrailing` 已准确拆为仍 Deferred 的 `cacheLimitTrailing` 与未来真实 Action 的 `clearCacheAction` 局部 region；未更改全页阈值、遮罩、rendered 或任何 golden。Dialog 仅登记 `11-dialog-components.png` 的已确认危险操作母版及约 32% 同页遮罩/状态规则，尚无 `SettingsClearCacheConfirmationDialogScreenshot` 或视觉结论；必须在下一代码切片补齐后再运行 fidelity 和人工复核。
- 2026-08-10 清理缓存 037 第一阶段：首次 screenshot validate 仅缺少 `SettingsClearCacheConfirmationDialogScreenshot_ClearCacheConfirmationDialog_5f6a1d1b_0.png` 与 `SettingsClearCacheConfirmationDialogLargeText20Screenshot_ClearCacheConfirmationDialogLargeText20_55367c2e_0.png` 两张新增参考图；其余 17 张既有基线未报差异，未更新任何 PNG。`settings.content.light` 使用现有默认页 rendered 的符合度为 `mean=0.028624413286902028`、`changed=0.04889314242384599`，仍在 `0.16 / 0.50` 阈值内。人工查看两个新增 rendered：`1.0×` 同页约 32% 黑色遮罩、Warning、Tonal 取消和 Destructive 清理操作符合确认组件；`2.0×` 标题、完整说明和两枚操作按钮均可见且未裁切。此结论只证明新增 Dialog 的候选视觉可批准，未代替后续授权 golden、重跑 validate 与完整视觉门禁。
- 2026-08-10 037 反馈一致性修复后：Success Snackbar 由 `LaunchedEffect` 在约 4 秒后发出一次 `DismissCacheClearFeedback`，不再永久驻留；partial/failed 与普通设置写入交错时，前者只在其结果最后完成时保留为可见的精确 Retry。重跑 screenshot validate 仍仅缺上述两张新增 reference，contract 20/20 通过；fidelity 数值不变。确认 Dialog 两个 rendered 未有结构或文字变化，仍维持上述人工结论。
- 2026-08-10 037 已获精确授权，仅新增 `SettingsClearCacheConfirmationDialogScreenshot_ClearCacheConfirmationDialog_5f6a1d1b_0.png`（SHA-256 `a0cb8ba372c448512fa4b24c307760fd88dee9afd9bab189c38c5f47ff0b23b1`）与 `SettingsClearCacheConfirmationDialogLargeText20Screenshot_ClearCacheConfirmationDialogLargeText20_55367c2e_0.png`（SHA-256 `08203332ce49318bceb5dae1bf5ce7b988fed0bf42269058122b6e707fa34733`）。reference 目录仅新增这两项，既有 PNG 未改动；随后 screenshot validate、contract、fidelity（`mean=0.028624413286902028`、`changed=0.04889314242384599`）及全部治理门禁通过。人工复看 `1.0×` / `2.0×` reference 与 rendered，确认遮罩、Warning、两种操作 tone、说明文案和大字号标题/按钮完整，无裁切或默认页漂移。

## 结论

- 外观、播放与音质、歌词、存储、其他五个分组及 14 个 Item 已完整恢复；分组宽度、圆角、标题、图标槽和分隔线按确认稿同画布复核。`播放失败时自动跳过`、`封面动态取色` 与“翻译与音译”使用确认稿同形态的开启 Switch；后两者分别只控制已解码封面的播放器派生色板与已加载歌词的 secondary 渲染，不改变应用系统动态主题、歌词请求或缓存。其余未接入能力的尾部区域按功能状态规则替换为“尚未开放”或禁用 Switch，不把确认稿示例值当作业务真值。
- 页面复用确认的全局居中 Toolbar：标题按 `10-user-profile` 母版实测使用 `15sp / 22sp`，返回图标保持共享 Chevron 轮廓、`16dp` 可见画布和 `48dp` 触控区；首个分组紧接 Toolbar。Item 使用一致的 `48dp` 最小触控高度且可随文字自然增高，长内容由单一 `LazyColumn` 滚动，没有按截图总高度压缩底部 Item。
- Light、Dark、AMOLED、`1.5×`、`2.0×`、`1040dp` 完整长内容、歌词字号、默认音质选择 Dialog 及其 `2.0×` 字体状态共 9 组截图基线已建立。默认音质激活所需的 7 张页面基线和新增 Dialog 基线均在设计符合度通过后受限更新；`2.0×` 默认音质 Dialog 的标题、七个选项和取消按钮均完整可见，首两项因放大后的自然换行增加高度但未裁切。Dialog 复用已确认的 `MoeDialog`、RadioButton 与取消按钮结构，七档不新造控件。
- 主题选择、关于 Dialog、播放失败自动跳过、封面动态取色、翻译与音译、歌词字体大小及默认音质保持真实交互；显示状态由 `SettingsUiState.overlay` 唯一持有，七个持久化写入各自独立保存、仅禁用自身，并在失败后回滚到最后持久化值且重试最后产生当前错误的项目。主题色复用现有 Radio Dialog，六档立即写入其既有全局 primary 角色消费者；默认音质只影响下一次登录态在线地址解析的偏好上限，不是当前实际质量；歌词字号只经 app 注入播放器纯 UI 输入，不触发歌词请求、映射或缓存；其余尚无消费者的设置使用明确禁用语义，不写入 DataStore、不展示假缓存容量，也不产生假成功反馈。
- `settings.content.light` 机器契约绑定确认稿、独立五锚点 probe、默认音质/播放失败自动跳过/翻译音译 Switch 的局部 region、两个选择 Dialog 的共享组件依据、功能状态测试和浅色/深色/AMOLED/两档大字体/长内容/Dialog 回归基线。
- 保留的 P3 差异仅为 Material 图标笔画与平台字体栅格；统一最小触控高度相对静态稿增加了页面滚动长度，这是无障碍约束，不改变信息密度层级。
