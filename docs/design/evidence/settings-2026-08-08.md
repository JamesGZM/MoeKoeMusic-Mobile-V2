# 设置页设计符合度证据

- 确认稿：`docs/design/mockups/09-settings.png`
- 实现截图：`feature/settings/src/screenshotTestDebug/reference/cn/james/music/feature/settings/SettingsScreenshotTestKt/SettingsLightScreenshot_Light_442d9280_0.png`
- 比较范围：确认稿 `853 × 2172px` 按实现截图宽度归一化，再裁取同高首屏对照；设置稿本身不绘制系统状态栏，完整长内容由 `SettingsLongContentScreenshot` 和真机滚动测试验收。公共 Toolbar 的标题、图标和光学基线另以 `10-user-profile.png` 母版复核。

## 证据文件

- `settings-2026-08-08-side-by-side.png`
- `settings-2026-08-08-overlay.png`
- `settings-2026-08-08-diff.png`

## 结论

- 外观、播放与音质、歌词、存储、其他五个分组及 14 个 Item 已完整恢复；分组宽度、圆角、标题、图标槽、分隔线、尾部值、Chevron 和 Switch 均按确认稿同画布复核。
- 页面复用确认的全局居中 Toolbar：标题按 `10-user-profile` 母版实测使用 `15sp / 22sp`，返回图标保持共享 Chevron 轮廓、`16dp` 可见画布和 `48dp` 触控区；首个分组紧接 Toolbar。Item 使用一致的 `48dp` 最小触控高度且可随文字自然增高，长内容由单一 `LazyColumn` 滚动，没有按截图总高度压缩底部 Item。
- Light、Dark、AMOLED、`1.5×`、`2.0×` 与 `1040dp` 完整长内容共 6 组截图基线已建立；大字体下文本和值未发生横向裁切。
- 主题选择与关于 Dialog 保持真实交互；其余尚无消费者的设置只展示确认稿静态状态，不写入 DataStore、不展示假缓存容量，也不产生假成功反馈。
- 保留的 P3 差异仅为 Material 图标笔画与平台字体栅格；统一最小触控高度相对静态稿增加了页面滚动长度，这是无障碍约束，不改变信息密度层级。
